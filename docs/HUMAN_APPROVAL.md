# Human Approval

Review must be requested before approval, rejection, or requested changes.
Every decision requires an approver identity and UTC timestamp; rejection and
changes require a reason. Decision history is immutable and audited.

Providers cannot approve their own work under the default policy. Approval
does not complete a task and does not merge a pull request. Merge and
deployment remain separate explicitly authorized operations.
